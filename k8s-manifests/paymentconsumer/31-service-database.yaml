apiVersion: v1
kind: Service
metadata:
  name: paymentconsumer-db
  namespace: default
  labels:
    app: paymentconsumer
    app.kubernetes.io/name: paymentconsumer
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: db
  selector:
    app: paymentconsumer
    component: database
